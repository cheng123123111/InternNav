# 调试记录

## 基本信息
- 仓库路径：`/root/code/InternNav`
- 当前分支：`qwen_should_stop_verifier`
- conda 环境：`internvln`
- 机器 GPU：8 x `NVIDIA A800-SXM4-80GB`
- 数据与权重根目录：`/dataset-vln`

## 这次问题的核心结论

### 1. 权重本身没有坏
最开始运行 `InternVLA-N1-DualVLN` checkpoint 时，报的是 `traj_dit` 里的线性层 shape mismatch：
- checkpoint: `1024 x 384`
- 当前构图: `1536 x 384`

一开始看起来像是权重版本不对，但后面定位下来，真正原因是 **`diffusers` 版本漂了**。

### 2. 真正根因是 `diffusers` 版本不匹配
当前代码里 `nextdit_traj.py` 使用的是 `diffusers.models.attention.LuminaFeedForward`。
不同版本的 `diffusers` 对 `inner_dim` 的处理不一样：
- `diffusers==0.33.1` 会走出 `1536`
- `diffusers==0.31.0` 会走出 `1024`

而 checkpoint 对应的是后者。

### 3. 修复方法
把环境从：
- `diffusers==0.33.1`
- `accelerate==1.4.0`

改成官方推理 notebook 对应版本：
- `diffusers==0.31.0`
- `accelerate==1.10.1`

修复后，checkpoint 加载 `mismatched_keys_count = 0`。

## 依赖与环境处理

### 已安装/确认可用的关键依赖
- `torch==2.6.0+cu124`
- `torchvision==0.21.0+cu124`
- `transformers==4.51.0`
- `flash-attn==2.7.4.post1`
- `habitat-sim==0.3.2`
- `habitat-lab==0.3.20231024`
- `internnav==0.3.1`
- `diffusers==0.31.0`
- `accelerate==1.10.1`

### 实际部署命令
```bash
eval "$(conda shell.bash hook)"
conda activate internvln

cd /root/code/InternNav

pip install torch==2.6.0 torchvision==0.21.0 --index-url https://download.pytorch.org/whl/cu124
pip install -e . --index-url https://pypi.org/simple
pip install -r requirements/core_requirements.txt --index-url https://pypi.org/simple
conda install -y habitat-sim=0.3.2 withbullet headless -c conda-forge -c aihabitat
pip install habitat-lab==0.3.20231024 --index-url https://pypi.org/simple
pip install -r requirements/habitat_requirements.txt --index-url https://pypi.org/simple
pip install -r requirements/internvla_n1.txt --index-url https://pypi.org/simple
git submodule update --init --recursive

pip install diffusers==0.31.0 accelerate==1.10.1 --index-url https://pypi.org/simple
```

### 额外处理
- 补了 OpenGL/EGL 相关运行时库，使 `habitat_sim` 能正常 import
- 下载了：
  - `/dataset-vln/InternNav/checkpoints/depth_anything_v2_metric_hypersim_vits.pth`
- 建了软链接：
  - `/root/code/InternNav/checkpoints -> /dataset-vln/InternNav/checkpoints`

## 配置与代码调整

### 保留的逻辑
- 保留了 `stop` 相关逻辑和入口
- baseline 与 stop 可以通过不同 config 切换

### 恢复过的配置项
之前为了省显存改过一些参数，已经恢复到原版推理规模：
- `resize_w/h: 320 -> 384`
- `max_new_tokens: 256 -> 1024`
- `attn_implementation: eager -> flash_attention_2`
- `torch_dtype: torch.bfloat16`
- `device_map` 明确绑定当前设备

### 当前保留的兼容性修改
#### 1. `nextdit_traj.py`
为了兼容当前 `diffusers`/梯度检查点接口，改了 `_set_gradient_checkpointing` 的函数签名，避免初始化时报错。

文件：
- [nextdit_traj.py](/root/code/InternNav/internnav/model/basemodel/internvla_n1/nextdit_traj.py)

#### 2. `habitat_vln_evaluator.py`
新增/保留了：
- stop verifier 逻辑
- 所有 episode 都保存视频
- 成功与失败视频分目录保存：
  - `success/`
  - `failed/`

文件：
- [habitat_vln_evaluator.py](/root/code/InternNav/internnav/habitat_extensions/vln/habitat_vln_evaluator.py)

### 本地新增的配置文件
- [habitat_dual_system_cfg_local.py](/root/code/InternNav/scripts/eval/configs/habitat_dual_system_cfg_local.py)
- [habitat_dual_system_cfg_local_video.py](/root/code/InternNav/scripts/eval/configs/habitat_dual_system_cfg_local_video.py)
- [habitat_dual_system_ep1_local_cfg.py](/root/code/InternNav/scripts/eval/configs/habitat_dual_system_ep1_local_cfg.py)
- [habitat_dual_system_first10_local_video_cfg.py](/root/code/InternNav/scripts/eval/configs/habitat_dual_system_first10_local_video_cfg.py)
- [vln_r2r_local.yaml](/root/code/InternNav/scripts/eval/configs/vln_r2r_local.yaml)
- [vln_r2r_ep1_local.yaml](/root/code/InternNav/scripts/eval/configs/vln_r2r_ep1_local.yaml)
- [vln_r2r_first10_local.yaml](/root/code/InternNav/scripts/eval/configs/vln_r2r_first10_local.yaml)
- [vln_r2r_val_unseen_first10.json.gz](/root/code/InternNav/scripts/eval/configs/vln_r2r_val_unseen_first10.json.gz)

## 数据路径确认

### 场景数据
- `/dataset-vln/VL-LN-Bench/scene_datasets/`

### VLN-CE 数据
- `/dataset-vln/vln_ce/raw_data/r2r/val_unseen/val_unseen.json.gz`

### 模型权重
- `/dataset-vln/InternNav/checkpoints/InternVLA-N1-DualVLN`
- `/dataset-vln/InternNav/checkpoints/clip-long/longclip-B.pt`

## 已验证跑通的命令

### 1. 单条 unseen 可视化 baseline
```bash
eval "$(conda shell.bash hook)"
conda activate internvln
cd /root/code/InternNav
python -u scripts/eval/eval.py --config scripts/eval/configs/habitat_dual_system_ep1_local_cfg.py
```

### 结果
- `success = 1.0`
- `spl = 0.9951026565473357`
- `os = 1.0`
- `ne = 0.40387263894081116`

视频路径：
- [0001.mp4](/root/code/InternNav/logs/habitat/ep1_local/vis_0/zsNo4HB9uLZ/0001.mp4)

## 当前可用启动命令

### 1. 前 10 条，8 卡，保存视频
```bash
eval "$(conda shell.bash hook)"
conda activate internvln
cd /root/code/InternNav

bash scripts/eval/bash/torchrun_habitat_eval.sh \
  --config scripts/eval/configs/habitat_dual_system_first10_local_video_cfg.py \
  --nproc_per_node 8
```

### 2. 全量 val_unseen，8 卡，保存视频
```bash
eval "$(conda shell.bash hook)"
conda activate internvln
cd /root/code/InternNav

bash scripts/eval/bash/torchrun_habitat_eval.sh \
  --config scripts/eval/configs/habitat_dual_system_cfg_local_video.py \
  --nproc_per_node 8
```

### 3. 监控 `progress.json`，每 10 条打印一次累计总统计
```bash
eval "$(conda shell.bash hook)"
conda activate internvln
cd /root/code/InternNav

python scripts/eval/monitor_habitat_progress.py \
  --progress logs/habitat/full_val_unseen_local_video/progress.json \
  --every 10
```

如果跑的是 first10 版本，把 `progress` 路径改成：
```bash
logs/habitat/first10_local_video/progress.json
```

## 视频保存规则
当前 evaluator 已改成：
- 所有 episode 都保存视频
- 成功与失败分目录

目录示例：
- `logs/habitat/full_val_unseen_local_video/vis_0/success/<scene_id>/0001.mp4`
- `logs/habitat/full_val_unseen_local_video/vis_0/failed/<scene_id>/0002.mp4`

## 多卡并行实验记录

### 1. 标准 8 卡方案
可用。

### 2. 尝试 16 进程共享 8 卡
做过两层修改：
- `habitat_vln_evaluator.py` 中设备映射改成 `local_rank % gpu_count`
- `internnav/utils/dist.py` 中 `torch.cuda.set_device(...)` 也改成取模

但是最终仍然失败，原因不是映射错误，而是 **NCCL 不允许同一个进程组里多个 rank 绑定同一张 GPU**。

典型报错：
- `Duplicate GPU detected`
- `ncclInvalidUsage`

结论：
- 当前这条 `torchrun + NCCL` 路线下，不能直接 `16` 个 rank 共享 `8` 张卡
- 老方案仍然应该保持 `8` 进程对 `8` 卡

## 启动脚本
文件：
- [torchrun_habitat_eval.sh](/root/code/InternNav/scripts/eval/bash/torchrun_habitat_eval.sh)

作用：
- 保留原始 bash 启动方式
- 支持：
  - `--config`
  - `--nproc_per_node`
  - `--master_port`

## 建议
如果后续觉得全量视频太慢，建议优先考虑：
- 先全量跑指标，不存视频
- 记录失败 episode
- 再单独重放失败样本存视频

因为当前瓶颈更偏向 Habitat 环境步进与串行交互，不是单纯 GPU 算力不足。


## 2026-04-03 Stop 元素覆盖训练

### 数据准备
- 原始 stop manifest：`/root/code/InternNav/tmp_stop_data/r2r_v1-3_stop_manifest_full.jsonl`
- 元素增强 v2：`/root/code/InternNav/tmp_stop_data/r2r_v1-3_stop_manifest_full_elements_v2.jsonl`
- 元素增强 v2 统计：
  - rows: `21286`
  - rows_with_target_elements: `19978`
  - rows_with_relation_hint: `6315`
  - element_coverage: `0.938551160387109`
- 失败视频打包：`/root/code/InternNav/exports/full_val_unseen_failed_videos.tar.gz`
- 失败视频清单：`/root/code/InternNav/exports/full_val_unseen_failed_videos.txt`

### 代码改动
- 更新了 `scripts/data_collect/stop_alignment_utils.py`
  - 修复 `bathroom` 中的 `at` 被误识别为 relation 的问题
  - 收紧动作词和弱目标词过滤
  - 改善多元素 stop phrase 的切分
- 新增 `scripts/data_collect/train_stop_element_longclip.py`
  - 基于 LongCLIP 的元素覆盖式 stop 头
  - 每张图对多个 `target_elements` 分别编码并计算相似度
  - 聚合统计：`mean_sim`、`max_sim`、`soft_coverage`、`count_ratio`
  - 训练损失：`BCE + rank loss`

### 训练命令
#### smoke test
```bash
eval "$(conda shell.bash hook)"
conda activate internvln
cd /root/code/InternNav
python scripts/data_collect/train_stop_element_longclip.py   --manifest /root/code/InternNav/tmp_stop_data/r2r_v1-3_stop_manifest_full_elements_v2.jsonl   --longclip-path /root/code/InternNav/checkpoints/clip-long/longclip-B.pt   --output-dir /root/code/InternNav/tmp_stop_phrase/run_longclip_element_smoke   --epochs 1   --batch-size 16   --max-samples 512   --lr 1e-4
```

#### full run
```bash
eval "$(conda shell.bash hook)"
conda activate internvln
cd /root/code/InternNav
python -u scripts/data_collect/train_stop_element_longclip.py   --manifest /root/code/InternNav/tmp_stop_data/r2r_v1-3_stop_manifest_full_elements_v2.jsonl   --longclip-path /root/code/InternNav/checkpoints/clip-long/longclip-B.pt   --output-dir /root/code/InternNav/tmp_stop_phrase/run_longclip_element_v1   --epochs 3   --batch-size 32   --lr 1e-4   > /root/code/InternNav/tmp_stop_phrase/run_longclip_element_v1.log 2>&1
```

### 当前结果
- smoke 目录：`/root/code/InternNav/tmp_stop_phrase/run_longclip_element_smoke`
- full 目录：`/root/code/InternNav/tmp_stop_phrase/run_longclip_element_v1`
- full 日志：`/root/code/InternNav/tmp_stop_phrase/run_longclip_element_v1.log`
- full run 已完成，共 `3` 个 epoch
- 指标：
  - epoch 0 val_acc: `0.7402536402066698`
  - epoch 1 val_acc: `0.733677782996712`
  - epoch 2 val_acc: `0.7552841709722875`
- 最终最好结果来自 `epoch 2`：
  - val_loss: `0.5294942199875906`
  - val_acc: `0.7552841709722875`
  - val_pos_raw_mean: `0.017109619936795085`
  - val_neg_raw_mean: `-0.4768333693701495`
- 当前 best checkpoint：`/root/code/InternNav/tmp_stop_phrase/run_longclip_element_v1/best_longclip_stop_elements.pt`
- 当前 last checkpoint：`/root/code/InternNav/tmp_stop_phrase/run_longclip_element_v1/last_longclip_stop_elements.pt`

### 备注
- 新版 `habitat_vln_evaluator.py` 已兼容两种 LongCLIP stop 权重：
  - 老版 projector checkpoint
  - 新版 element-coverage checkpoint（`best_longclip_stop_elements.pt`）
- 新版 element 模式会从 instruction 提取 `target_elements`，对每个元素分别计算图文相似度，再聚合 `mean/max/soft_coverage/count_ratio` 送入 score head。
- 下一步可以直接配一份 `longclip element stop` 的 eval config 做 smoke test。


## 2026-04-03 Stop 元素训练最终结果
- full run 已完成，共 `3` 个 epoch
- best 来自 `epoch 2`
- final val_acc: `0.7552841709722875`
- final val_loss: `0.5294942199875906`
- best checkpoint: `/root/code/InternNav/tmp_stop_phrase/run_longclip_element_v1/best_longclip_stop_elements.pt`
- last checkpoint: `/root/code/InternNav/tmp_stop_phrase/run_longclip_element_v1/last_longclip_stop_elements.pt`
- `habitat_vln_evaluator.py` 已兼容新版 element-coverage LongCLIP checkpoint
- 新增 smoke config: `/root/code/InternNav/scripts/eval/configs/habitat_dual_system_ep1_longclipelement_cfg.py`

## 2026-04-03 继续中的 5 帧 history + action 分支
- 新增 `scripts/data_collect/build_stop_manifest_from_traj_tar.py`
  - 支持 `--history-frames`
  - 样本中会写入 `frame_paths` 和 `history_frame_count`
- 新增 `scripts/data_collect/enrich_stop_manifest_with_elements.py`
  - 增强字段：`target_elements`、`target_elements_text`、`relation_hint`、`stop_action_text`
- 新增 `scripts/data_collect/train_stop_element_longclip_history.py`
  - 支持最近 `5` 帧聚合
  - 支持元素分支和动作/关系文本分支
- 相关 smoke 已跑通：
  - `/root/code/InternNav/tmp_stop_phrase/run_longclip_element_history5_smoke`
  - `/root/code/InternNav/tmp_stop_phrase/run_longclip_element_history5_action_smoke`
- 全量 `history=5` 数据生成命令：
```bash
eval "$(conda shell.bash hook)"
conda activate internvln
cd /root/code/InternNav
python scripts/data_collect/build_stop_manifest_from_traj_tar.py \
  --traj-root /dataset-vln/vln_ce/traj_data/r2r_v1-3 \
  --output /root/code/InternNav/tmp_stop_data/r2r_v1-3_stop_manifest_history5_full.jsonl \
  --frames-root /root/code/InternNav/tmp_stop_data/frames_history5_full \
  --negative-offsets 8,16 \
  --history-frames 5
```
